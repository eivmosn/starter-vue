# starter-vue

## env file

If you want to override the default env file, you can create a new file named `.env.local` in the root directory of the project.

Do not commit the `.env.local` file to the repository.

Do not change the default `.env` file and just override the variables you need.
